// PostCSS: pipeline de Tailwind v3 + autoprefixer.
// No editar salvo que agregues plugins de PostCSS adicionales.
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
